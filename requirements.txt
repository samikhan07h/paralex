# --- Phase 1: Core RAG Pipeline ---

# PDF / document parsing
pypdf==4.3.1
python-docx==1.1.2
pdfplumber==0.11.4  # table extraction (Phase 3) — see src/ingestion/table_extractor.py

# Chunking / orchestration
langchain==0.2.16
langchain-community==0.2.16
langchain-text-splitters==0.2.4

# Embeddings (local, free)
sentence-transformers==3.0.1

# Vector store
faiss-cpu>=1.9.0

# LLM provider (Groq — free tier, fast inference)
groq>=1.0.0

# Config / environment
python-dotenv==1.0.1
pydantic==2.9.2

# --- Later phases (kept here so requirements.txt doesn't need repeated edits) ---
streamlit==1.38.0            # Phase 4 — UI
# ragas==0.1.16              # Phase 2 — faithfulness/answer-relevance scoring
# pandas==2.2.2               # Phase 2/3 — eval result tables

# --- Dev / testing ---
pytest==8.3.3
